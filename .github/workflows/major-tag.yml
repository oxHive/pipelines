name: Major Tag

on:
  workflow_dispatch:
  # push:
  #   tags:
  #     - "v*.*.*"

jobs:
  major-tag:
    uses: ./.github/workflows/shared-major-tag.yml
    secrets:
      MAJOR_TAG_TOKEN: ${{ secrets.MAJOR_TAG_TOKEN }}
