name: Major Tag

on:
  workflow_dispatch:
  # push:
  #   tags:
  #     - "v*.*.*"

jobs:
  major-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: $/.github/actions/major-tag
        with:
          token: ${{ secrets.MAJOR_TAG_TOKEN }}
